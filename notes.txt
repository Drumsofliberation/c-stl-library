#ifndef ARRAY_H      // "if not defined ARRAY_H"
#define ARRAY_H      // "define ARRAY_H"

// ... your code here ...

#endif               // end of the guard


// main.c
#include "array.h"
#include "utils.h"  // utils.h also includes array.h

// Without header guards, array.h gets included TWICE!
// This causes "redefinition" errors

------------------------------------------------------------
Macro Function
Replaces text while pre-processing (before compling) with the macro body
Acts as a wrapper function here

#define array_find(arr,value)( array_find_impl(arr, sizeof(arr)/sizeof((arr)[0]), value) )

------------------------------------------------------------

Doxygen Docs

/**
 * @brief Finds a value in an int array
 * @param arr Array to search in
 * @param n Size of the array
 * @param value Value to find
 * @return Index of the value, or -1 if not found
 */

------------------------------------------------------------

